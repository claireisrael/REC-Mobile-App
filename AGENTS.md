# REC Mobile App

Expo client for the **same** REC app as `../rec-registration/`. Same version, same data, same APIs — native UI only. See `.cursor/rules/web-to-mobile-parity.mdc`.

Docs: https://docs.expo.dev/versions/v54.0.0/ (project uses SDK 54).
